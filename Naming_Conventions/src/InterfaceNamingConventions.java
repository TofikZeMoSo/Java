public class InterfaceNamingConventions {

    interface IShapeFactory{
        public void circle();

    }
    interface ShapeFactoryImp
    {
        public void circle();
    }
// above two interfaces and their names are right normally ppl use Ishapefacotry as their interface name
// but if u don't want to know user that it's interface then u can also use ShapeFactoryImp as ur interface name
}
