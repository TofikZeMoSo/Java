public class Meaningfull_Distinctions {
    //    private int klass;          all the examples given near are wrong and does not follow the meaningful distinctions
//    private String variable;    u can not use klass as name just because class is already used
//    private int a1, a2, a3;     also naming any variable as variable is wrong practice.
    // you must not use Number-series naming because it's not always helpful

}
class Product
{
    //Noise words are another meaningless distinction.
}
class ProductInfo{

    // here info is noise word
    //The word variable should never appear in a variable name. The word table should never appear in a table name

}
//Distinguish names in such a way that the reader knows what the differences offe
}
